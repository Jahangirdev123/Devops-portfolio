name: HEllo World
on:
  push:
    branches: ["main"]
jobs:
  run:
    runs-on: ubuntu-slim
    steps:
      - name: Step 1
        uses: actions/checkout@v4
      - name: Step 2
        run:  echo "another try"
