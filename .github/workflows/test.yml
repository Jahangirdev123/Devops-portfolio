name: Myapp
on:
  push:
    branches: ["main"]
jobs:
  print-message: 
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        uses: actions/checkout@v4
      - name: Step 2
        run: echo "Hello World"
